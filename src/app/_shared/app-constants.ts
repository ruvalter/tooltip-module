export enum WindowEvents {
  click = 'click',
  scroll = 'scroll',
  keyup = 'keyup',
  keydown = 'keydown'
}

export enum KeyboardCodes {
  escape = 27
}

export enum ButtonType {
  a = <any>'a',
  b = <any>'b'
}